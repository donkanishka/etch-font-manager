name: Lint

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  php:
    name: PHP ${{ matrix.php }}
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        php: ['7.4', '8.3']

    steps:
      - uses: actions/checkout@v4

      - name: Set up PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: ${{ matrix.php }}
          coverage: none
          tools: cs2pr

      - name: Syntax check
        run: find . -path ./vendor -prune -o -name '*.php' -print0 | xargs -0 -n1 -P4 php -l

  phpcs:
    name: Coding standards
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.3'
          coverage: none
          tools: composer, cs2pr

      - name: Install PHPCS and the WordPress standards
        run: |
          composer global config --no-plugins allow-plugins.dealerdirect/phpcodesniffer-composer-installer true
          composer global require --no-interaction --no-progress \
            squizlabs/php_codesniffer \
            wp-coding-standards/wpcs:^3 \
            phpcompatibility/phpcompatibility-wp
          echo "$HOME/.composer/vendor/bin" >> "$GITHUB_PATH"

      # Warnings, which are almost all whitespace alignment, are surfaced as
      # annotations but do not block a release. Run `phpcbf` locally to apply
      # them. Errors are fatal, and are checked separately so that the exit
      # code cannot be masked by the pipe.
      - name: Collect coding standard report
        run: phpcs -q --report=checkstyle > phpcs-report.xml || true

      - name: Annotate the pull request
        run: cs2pr --graceful-warnings phpcs-report.xml

      - name: Fail on errors
        run: phpcs -q --warning-severity=0

  javascript:
    name: JavaScript syntax
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Check panel script parses
        run: node --check assets/panel.js

      - name: Check the update manifest is valid JSON
        run: node -e "JSON.parse(require('fs').readFileSync('update.json','utf8')); console.log('update.json OK')"
